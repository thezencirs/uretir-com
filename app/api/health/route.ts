import {NextResponse} from "next/server";import {db} from "@/lib/members/store";export const dynamic="force-dynamic";export async function GET(){try{const r=await db().query("SELECT to_regclass('member_accounts') IS NOT NULL AND to_regclass('member_content') IS NOT NULL AND to_regclass('haber_articles') IS NOT NULL AS ready");const ready=!!r.rows[0]?.ready;return NextResponse.json({status:ready?"ok":"unavailable"},{status:ready?200:503,headers:{"Cache-Control":"no-store"}});}catch{return NextResponse.json({status:"unavailable"},{status:503});}}
