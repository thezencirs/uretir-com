import type {Metadata} from "next";import {StartupDirectory} from "@/components/startup-directory";import {publicContent} from "@/lib/members/store";export const dynamic="force-dynamic";export const metadata:Metadata={title:"Girişim haritası",description:"Türkiye girişim ekosistemi: şehirler, ürünler ve kaynaklı finansal görünüm.",alternates:{canonical:"/ekosistem/harita"},};
export default async function Page({searchParams}:{searchParams:Promise<{city?:string}>}){const {city}=await searchParams;const rows=await publicContent("startup");const entries=rows.map(e=>({id:e.id,name:e.payload.name!,cityCode:e.payload.cityCode!,description:e.payload.description!,website:e.payload.website??"",product:e.payload.product!,reviewedAt:e.published_at??""}));return <StartupDirectory mode="harita" startupEntries={entries} initialCity={city}/>;}