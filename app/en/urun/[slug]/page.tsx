import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {getProductEntry} from "@/lib/product-entry";import {catalogText,englishLabels} from "@/lib/catalog-language";
export const dynamic="force-dynamic";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const entry=await getProductEntry(slug);return entry?{title:entry.product.name,description:catalogText(entry.product.description,true),alternates:{canonical:"/en/urun/"+slug,languages:{tr:"/urun/"+slug,en:"/en/urun/"+slug}}}:{};}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const entry=await getProductEntry(slug);if(!entry)notFound();const {product:p,maker:m}=entry;return <div className="section-wrap py-14 md:py-24"><Link href="/en" className="text-sm underline">← All products</Link><div className="mt-10 grid gap-10 md:grid-cols-[1.3fr_.7fr]"><div><p className="text-sm text-muted">{catalogText(p.category,true)} / {catalogText(p.task,true)}</p><h1 className="mt-5 font-display text-5xl md:text-7xl">{p.name}</h1><p className="mt-6 text-xl leading-8 text-muted" lang={englishLabels[p.description]?"en":"tr"}>{catalogText(p.description,true)}</p>{!englishLabels[p.description]&&<p className="mt-3 text-sm text-muted">Description provided in Turkish.</p>}<p className="mt-6">{m.name} · {m.city}, Türkiye</p><a href={p.url} target="_blank" rel="noreferrer" className="mt-8 inline-block rounded-lg bg-[#28452f] px-6 py-4 text-white">Official product site and store links ↗</a><p className="mt-4 text-sm text-muted">Check the maker’s official site for availability, pricing and platforms.</p></div><aside className="border hairline p-7"><h2 className="text-xl font-semibold">Go to the source</h2><p className="mt-4 text-base leading-7 text-muted">This entry links to official product and maker sources. Location refers to the team, not company ownership.</p><a className="mt-5 block underline" href={m.source} target="_blank" rel="noreferrer">Product source ↗</a><a className="mt-5 block underline" href={m.locationSource} target="_blank" rel="noreferrer">Location source ↗</a><Link className="mt-5 block underline" href={"/en/harita?sehir="+encodeURIComponent(m.city)+"&q="+encodeURIComponent(m.name)}>Find the maker on the map ↗</Link></aside></div></div>}
